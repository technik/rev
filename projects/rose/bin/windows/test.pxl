uniform vec3 lightDir;

varying vec3 vNormal;

void main (void)
{
	vec3 finalNormal = normalize(vNormal);
	float diffColor = - finalNormal * normalize(lightDir);
	vec3 color = vec3(0.3+ clamp(diffColor, 0.0, 0.7));
	gl_FragColor = vec4(color, 1.0);
}