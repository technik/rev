
uniform vec3 lightDir;

varying vec3 vNormal;

void main (void) {
	vec3 lightColor = vec3(0.9,0.9,1.0);
	vec3 ambient = vec3(0.2,0.2,0.225);
	vec3 light = normalize(vec3(1.8, -1.0, 2.2));
	float diff = dot(vNormal, light);
	diff = max(0.0,diff);
	gl_FragColor = vec4(ambient+lightColor*diff,1.0);
}