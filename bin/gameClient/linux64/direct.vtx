uniform mat4 modelViewProj;
attribute vec3 vertex;
attribute vec4 color;

void main ( void )
{
	vec4 vertexPos;
	vertexPos.xyz = vertex.xyz;
	vertexPos.w = 1.0;
	gl_Position = modelViewProj * vertexPos;
}
