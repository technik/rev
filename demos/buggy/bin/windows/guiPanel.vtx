uniform mat4 modelViewProj;
attribute vec3 vertex;
attribute vec2 uv0;
varying vec2 vTex0;
varying vec3 vPos;

void main ( void )
{
	vPos = vertex;
	vTex0 = uv0;
	gl_Position = modelViewProj * vec4(vertex, 1.0);
}