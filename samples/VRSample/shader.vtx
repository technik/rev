//----------------------------------------------------------------------------------------------------------------------
// Basic shader
//----------------------------------------------------------------------------------------------------------------------
// Vertex
uniform	mat4	uWorldViewProj;
attribute vec3 vertex;

void main ( void )
{
	//vPos = vertex;
	gl_Position = uWorldViewProj * vec4(vertex, 1.0);
}