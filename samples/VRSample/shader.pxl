//----------------------------------------------------------------------------------------------------------------------
// Basic shader
//----------------------------------------------------------------------------------------------------------------------
// Pixel
uniform vec3 lightDir; // In object space
uniform vec3 lightClr;

//varying vec3 vNormal; // In object space

void main (void) {
//	float intensity = max(0.0, dot(lightDir, vNormal));
	vec3 color = vec3(0.0,0.0,1.0);// * lightClr *intensity;
	gl_FragColor = vec4(color, 1.0);
}