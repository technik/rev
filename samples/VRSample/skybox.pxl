//----------------------------------------------------------------------------------------------------------------------
// Basic shader
//----------------------------------------------------------------------------------------------------------------------
// Pixel shader
varying		vec2 vUV;

uniform sampler2D uSkyMap;

void main (void) {
	gl_FragColor = texture(uSkyMap, vUV);
	//gl_FragColor = vec4(vec3(vUV.x, vUV.y, 0.0), 1.0);
}
